/* Names: Store the names of a few of your friends in a array called names. Print each person’s name by
accessing each element in the list, one at a time. */

let friends_name = ["Ahtisham", "Usman", "Hamza", "Husnain"]
for (let c = 0; c < friends_name.length; c++) {
     console.log(friends_name[c])
}

export default friends_name